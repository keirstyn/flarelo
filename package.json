{
  "name": "flarelo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "vitest": "^2.1.0",
    "@cloudflare/vitest-pool-workers": "^0.6.0"
  }
}
